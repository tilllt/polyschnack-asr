# PolySchnack ASR (Parakeet TDT 0.6B ONNX) - CPU Dockerfile
# High-performance speech transcription on CPU

FROM python:3.10-slim

LABEL maintainer="PolySchnack"
LABEL description="ONNX-optimized speech transcription service (CPU)"

# Prevent interactive prompts during build
ENV DEBIAN_FRONTEND=noninteractive

# Install system dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    ffmpeg \
    && rm -rf /var/lib/apt/lists/* \
    && apt-get clean

# Create non-root user for security
RUN useradd --create-home --shell /bin/bash polyschnack
WORKDIR /app

# Copy requirements first for layer caching
COPY requirements.txt .

# Install Python dependencies. requirements.txt is GPU-first, so the CPU image
# swaps onnxruntime-gpu for onnxruntime.
RUN pip install --no-cache-dir --upgrade pip && \
    grep -v '^onnxruntime-gpu' requirements.txt > /tmp/requirements.cpu.txt && \
    pip install --no-cache-dir -r /tmp/requirements.cpu.txt && \
    pip install --no-cache-dir onnxruntime==1.28.0

# Copy application code
COPY server.py .
COPY polyschnack_service/ polyschnack_service/
COPY app.py .
COPY templates/ templates/
COPY polyschnack.png .

# Create directories for runtime
RUN mkdir -p temp_uploads models && \
    chown -R polyschnack:polyschnack /app

# Switch to non-root user
USER polyschnack

# Environment variables
ENV HF_HOME=/app/models
ENV HF_HUB_CACHE=/app/models
ENV HF_HUB_DISABLE_SYMLINKS_WARNING=true
ENV PYTHONUNBUFFERED=1
ENV POLYSNACK_USE_GPU=false
ENV POLYSNACK_DEFAULT_MODEL=parakeet-tdt-0.6b-v3
ENV POLYSNACK_BATCHED=0

# Expose the API port
EXPOSE 5092

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:5092/health')" || exit 1

# Run the application
CMD ["python", "server.py"]
