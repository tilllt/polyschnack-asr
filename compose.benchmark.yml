# PolySchnack — Benchmark-Container (periodisch per Cron auf dem Host).
#
# Aktivierung (startet einmalig, Container endet nach dem Lauf):
#   docker compose -f compose.yml -f compose.benchmark.yml run --rm benchmark
#
# Periodisch per Host-Cron (z. B. täglich 04:00):
#   0 4 * * * cd /pfad/zum/polyschnack-checkout && docker compose -f compose.yml -f compose.benchmark.yml run --rm benchmark >> /var/log/polyschnack-benchmark.log 2>&1
#
# Der Container:
#   - liest das NEUE versionierte Manifest aus dem gemeinsamen Volume
#     (versions/vN/manifest.json, aktive Samples)
#   - schickt jedes Sample an die konfigurierten Backends (OpenAI-kompatibel)
#   - schreibt results/latest.json + pricing.json ins Volume (/data/benchmark)
#   - Die Webapp zeigt die Ergebnisse auf /benchmark an — kein Neustart nötig.
#
# KEINE GPU nötig (Backends laufen separat) — der Container ist CPU-only und
# endet nach dem Lauf (kein Leerlauf-Ressourcenverbrauch).

services:
  benchmark:
    # Benchmark-Image liegt nur in der privaten Dev-Registry ->
    # REGISTRY-Override setzen (z. B. auf den internen Registry-Host).
    image: ${REGISTRY}/polyschnack-benchmark:latest
    container_name: ps-benchmark
    # Einmal-Lauf: `run --rm` erzeugt und entfernt den Container automatisch.
    restart: "no"
    environment:
      BENCHMARK_DATA_DIR: /data/benchmark
      # Backends + URLs im Compose-Netzwerk (Container-Port, nicht Host-Port!)
      # Beide Variablen per .env ueberschreibbar (BENCH_BACKEND_URLS als
      # JSON-Map) — so laeuft der Benchmark auch gegen externe
      # OpenAI-kompatible Endpunkte (z. B. api.openai.com/v1).
      BENCH_BACKENDS: "${BENCH_BACKENDS:-ps-pk-onnx,crispr-qwen3,crispr-ark,crispr-moonshine-de,crispr-canary,crispr-pk-cpp}"
      # JSON-Map Name->URL. KEIN :-Default hier moeglich (JSON enthaelt '}',
      # das Compose-Interpolation nicht in Defaults unterstuetzt) — der
      # Default steht in polyschnack-manage.sh (benchmark) bzw. in der .env.
      # Beispiel extern: BENCH_BACKEND_URLS='{"whisper-remote":"https://api.openai.com/v1"}'
      BENCH_BACKEND_URLS: '${BENCH_BACKEND_URLS}'
      # API-Keys (optional): fuer externe OpenAI-kompatible Endpunkte sendet
      # run.py dann Authorization: Bearer <key> mit (OpenAI/Mistral/Groq/…).
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
      LITELLM_API_KEY: "${LITELLM_API_KEY:-}"
    volumes:
      # Least-Privilege: Nur das Benchmark-Verzeichnis ist beschreibbar
      # (latest.json + pricing.json), der Rest des Daten-Volumes bleibt read-only.
      - ./DATA/poc-data:/data:ro
      - ./DATA/poc-data/benchmark:/data/benchmark:rw
    networks:
      - default
