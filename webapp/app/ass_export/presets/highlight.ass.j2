{#- Hervorhebung: ein Event pro Wort — das gesprochene Wort farbig, die anderen grau. -#}
{% import "_base.ass.j2" as base %}{% call base.document(meta, styles) -%}
{% for st in steps -%}
Dialogue: 0,{{ st.start_tc }},{{ st.end_tc }},Default,{{ st.speaker }},0,0,0,,{% if params.fade_ms %}{\fad({{ params.fade_ms }},{{ params.fade_ms }})}{% endif %}{% for w in st.words %}{% if w.is_active %}{\c&H{{ params.accent_color | bgr }}&}{% else %}{\c&H{{ params.dim_color | bgr }}&}{% endif %}{{ w.text | ass_escape }}{% if not loop.last %} {% endif %}{% endfor %}
{% endfor -%}
{%- endcall %}
